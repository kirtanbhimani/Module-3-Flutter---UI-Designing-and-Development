import 'package:flutter/material.dart';

class Question50 extends StatelessWidget {
  const Question50({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Question 50'),
        backgroundColor: Colors.pinkAccent,
      ),
      
    );
  }
}
